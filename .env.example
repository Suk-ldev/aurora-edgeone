# 环境变量只有这一个，站点配置在 site.config.js
# - EdgeOne：控制台「项目设置 → 环境变量」添加，改完需要重新部署
# - 本地开发：复制本文件为 .env.local 再填写（.env.local 不会被提交）

# 后端地址（必填）。只在边缘函数里使用，不会写进前端代码，用户看不到
# 本地 npm run dev 时 /api 和 /s 也会转发到这里
API_URL=https://your-xboard-backend.com
